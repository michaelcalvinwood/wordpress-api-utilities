import './GrapesEditor.scss';

const GrapesEditor = props => {
    const { content, updateContent } = props;

    return (
        <div className="grapes-editor">
            Hello GrapesEditor
        </div>
    )
}

export default GrapesEditor;